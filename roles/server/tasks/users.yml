---
- include_role:
    name: diffy0712.sysadmin_user_manager
  vars:
    sysadmin_user: "{{ sysadmins.ansible.sysadmin_user }}"
    sysadmin_password: "{{ sysadmins.ansible.sysadmin_password }}"
    sysadmin_shell: "{{ sysadmins.ansible.sysadmin_shell }}"
    sysadmin_issudoer: "{{ sysadmins.ansible.sysadmin_issudoer }}"
    sysadmin_keys: "{{ sysadmins.ansible.sysadmin_keys }}"

- include_role:
    name: diffy0712.sysadmin_user_manager
  vars:
    sysadmin_user: "{{ sysadmins.sysadmin.sysadmin_user }}"
    sysadmin_password: "{{ sysadmins.sysadmin.sysadmin_password }}"
    sysadmin_shell: "{{ sysadmins.sysadmin.sysadmin_shell }}"
    sysadmin_issudoer: "{{ sysadmins.sysadmin.sysadmin_issudoer }}"
    sysadmin_keys: "{{ sysadmins.sysadmin.sysadmin_keys }}"